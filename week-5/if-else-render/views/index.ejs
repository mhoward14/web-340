<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Exercise 5.2 - if-else-render</title>
</head>
<body>

<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <ul class="navbar-nav">
        <li class="nav-item active">
            <a class="nav-link" href="#">Home</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">About</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">Contact Us</a>
        </li>
    </ul>
</nav>

<div role="main" class="container"  >

        <br/><br/>
        
        <h2>Names List</h2>
        
        <% for (var k = 0; k < names.length; k++) { %>
        
        <!-- if -->
        
        <% if (names[k] == "Bobby Charlton") { %>
        
        <div class="alert alert-success" role="alert"><strong><%= names[k] %></strong></div>
        
        <% } %>
        
        <!-- // end if -->
        
        <!-- if -->
        
        <% if (names[k] == "George Best") { %>
        
        <div class="alert alert-info" role="alert"><strong><%= names[k] %></strong></div>
        
        <% } %>
        
        <!-- // end if -->
        
        <!-- if -->
        
        <% if (names[k] == "Ryan Giggs") { %>
        
        <div class="alert alert-warning" role="alert"><strong><%= names[k] %></strong></div>
        
        <% } %>
        
        <!-- // end if -->
        
        <!-- if -->
        
        <% if (names[k] == "Eric Cantona") { %>
        
        <div class="alert alert-danger" role="alert"><strong><%= names[k] %></strong></div>
        
        <% } %>
        
        <!-- // end if -->
        
        <% } %>
        
        <!-- end for loop -->
        
        </div>